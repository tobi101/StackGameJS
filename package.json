{
  "name": "StackGame",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "deploy": "gh-pages -d dist"
  },
  "private": true,
  "devDependencies": {
    "vite": "^6.0.11"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "three": "^0.173.0"
  }
}
